# =============================================================================
# Network interfaces
# =============================================================================
#
# NetworkManager manages every physical interface; ifupdown keeps only
# loopback.
#
# Path:  /etc/network/interfaces
# Apply: reboot

source /etc/network/interfaces.d/*

auto lo
iface lo inet loopback
