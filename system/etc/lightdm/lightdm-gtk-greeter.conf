# =============================================================================
# LightDM — login screen
# =============================================================================
#
# lightdm-gtk-greeter with the GraphiteBlue-LightDM GTK theme from
# /usr/share/themes.
#
# Path:  /etc/lightdm/lightdm-gtk-greeter.conf
# Apply: log out

[greeter]
theme-name=GraphiteBlue-LightDM
icon-theme-name=Adwaita
background=#111318
user-background=false
transition-duration=0
transition-type=none

font-name=Ubuntu Bold 16
xft-antialias=true
xft-dpi=96
xft-hintstyle=hintslight
xft-rgba=rgb

position=50%,center 50%,center
hide-user-image=true
highlight-logged-user=true

# The monitor under the mouse pointer, whatever the outputs are called.
active-monitor=#cursor

panel-position=top
indicators=~session;~spacer;~layout;~clock;~power
clock-format=%d.%m.%Y  %H:%M
keyboard-layouts=us;ru

# Blank the screen after 10 minutes when LightDM is used as the lock screen.
screensaver-timeout=600
allow-debugging=false

# Used until the user picks another session.
default-session=i3
