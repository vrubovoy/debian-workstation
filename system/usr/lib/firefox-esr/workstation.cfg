// =============================================================================
// Firefox — AutoConfig script
// =============================================================================
//
// Applies workstation-newtab.css to every profile, including new ones.
// AutoConfig skips the first line of this file, so it must stay a comment.
//
// Path:  /usr/lib/firefox-esr/workstation.cfg
// Apply: restart Firefox

const sss = Components.classes["@mozilla.org/content/style-sheet-service;1"]
    .getService(Components.interfaces.nsIStyleSheetService);
const css = Components.classes["@mozilla.org/network/io-service;1"]
    .getService(Components.interfaces.nsIIOService)
    .newURI("file:///usr/lib/firefox-esr/workstation-newtab.css");

if (!sss.sheetRegistered(css, sss.USER_SHEET)) {
    sss.loadAndRegisterSheet(css, sss.USER_SHEET);
}
